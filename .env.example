# ── Database ──
DATABASE_URL=postgresql://postgres:password@localhost:5432/LmsDb

# ── JWT Configuration ──
JWT_KEY=YourSecretKeyAtLeast32BytesLong!
JWT_ISSUER=LmsApi
JWT_AUDIENCE=LmsApiUsers
JWT_EXPIRY_IN_HOURS=3

# ── Redis Cache (Optional) ──
REDIS_URL=redis://localhost:6379

# ── Google OAuth (Optional) ──
GG_CLIENT_ID=your_google_client_id

# ── Docker Development ──
POSTGRES_PASSWORD=YourPassword123
